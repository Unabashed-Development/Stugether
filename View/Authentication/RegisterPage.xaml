<Page x:Class="View.Authentication.RegisterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:View.Authentication"
      mc:Ignorable="d" 
      Title="RegisterPage">

    <Grid>
        <Border Background="#94D2E6" CornerRadius="10" Margin="10">
            <StackPanel Margin="10">
                <Label Content="E-mailadres van je schoolaccount" FontSize="14"/>
                <TextBox Name="username" Margin="5 0" FontSize="14"/>
                <Label Content="Je wachtwoord" FontSize="14"/>
                <TextBox Name="password" Margin="5 0" FontSize="14"/>
                <Button Name="registerSubmit" Content="Registreren" Margin="100 10" Background="#F16F6F" Foreground="White" FontSize="14">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Separator/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Label Content="Terug naar inloggen?" FontSize="14" HorizontalAlignment="Center" Margin="0,0,-5,0"/>
                    <Label Content="Klik hier!" FontSize="14" HorizontalAlignment="Center" FontWeight="Bold" MouseLeftButtonDown="LoginClick"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
